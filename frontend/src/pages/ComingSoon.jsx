import "../App.css";

export default function ComingSoon({ title }) {
  return (
    <div className="coming-page">
      <h1>{title}</h1>
      <p>This module is planned for ClearSOC. It will be connected to live investigation data later.</p>
    </div>
  );
}
